<div><hr class="bg-warning">
  <div class="row">
    <div class="col-md-5">
      <h2>Course Name: <%= @course.name.capitalize %></h2>
    </div>
    <div class="col-md-5">
      <h2>Hours: <%= @course.hours %></h2>
    </div>
  </div>
</div><hr class="bg-warning">

<table class="table table-hover table-striped">
  <thead>
    <tr class="tb-header">
      <th>
        Cohorts: 
      </th>
      <th>
        Start date:
      </th>
      <th>
        End date:
      </th>
       <th>
      </th>
      <th>
        <button class="btn btn-sm btn-warning"><%= link_to 'Add New', new_cohort_path(@cohorts) %>
        </button>
      </th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: "show", locals: {courses: @courses} %>
  </tbody>
</table>
