
  <tr>
    <td>
      <%= @course.name %>
    </td>
    <td>
      <%= @course.hours %>
    </td> 
    <tr class="tb-header">
      <th>
        Cohorts:
      </th>
      <th>
      </th>
      <th>
      </th>
    </tr>
    <% @course.cohorts.each do |cohort| %>
      <tr>
        
        <td>
          <h5><%= cohort.name %></h5> <hr>
        </td>
        <td>
          <%= link_to "Edit", edit_course_path(@course), class: "glyphicon glyphicon-pencil index-icons" %>
        </td>
        <td>
          <%= link_to 'Delete', course_path(@course), method: :delete, class: 'glyphicon glyphicon-trash index-icons', id: "#{@course.id}", data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
    <%# <button> Add New Cohort<% link_to new_cohort_path(@cohort) %>
    </button> %>
    
    <td> 
      <%= link_to "Add Cohort", new_cohort_path(@cohort), class: 'glyphicon glyphicon-plus index-icons' %>
    </td>  
  </tr>


